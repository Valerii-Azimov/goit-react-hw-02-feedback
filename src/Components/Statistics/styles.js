import styled from 'styled-components';

const Statistic = styled.div`
  display: flex;
  flex-direction: column;
  margin-top: 10px;
`;
const Good = styled.p``;
const Neutral = styled.p``;
const Bad = styled.p``;
const Total = styled.p``;
const PositiveFedback = styled.p``;
const MessageError = styled.p``;

export { Statistic, Good, Neutral, Bad, Total, PositiveFedback, MessageError };
